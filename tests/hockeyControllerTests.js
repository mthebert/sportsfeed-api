const should = require('should');
const sinon = require('sinon');
const hockeyController = require('../controllers/hockeyController');

describe('Hockey Controller Tests', () => {
  describe('Get Tests', () => {
    return;
  });
});